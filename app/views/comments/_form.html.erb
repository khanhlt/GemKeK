<h3>Post a Comment</h3>
<%= form_for [ @review, @comment ] do |f| %>
  <p>
  </p>
  <p>
    <%= f.label :content %><br/>
    <%= f.text_area :content %>
  </p>
  <p>
    <%= f.submit 'Submit' %>
  </p>
<% end %>