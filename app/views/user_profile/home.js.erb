$("#abc_reviews").append('<%= j render @review %>');
<% if @reviews.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @reviews %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>